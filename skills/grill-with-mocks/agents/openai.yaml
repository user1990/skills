interface:
  display_name: "Grill With Mocks"
  short_description: "Resolve decisions with plain-language frontier rounds"
  default_prompt: "Use $grill-with-mocks to resolve this idea through short plain-language frontier rounds, with recommendations and the smallest useful decision, visual, or capture artifact."

policy:
  allow_implicit_invocation: false
